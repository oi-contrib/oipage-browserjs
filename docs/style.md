# 样式操作

## 获取指定样式

```js
bjs.css(key: string): string
```

## 设置指定样式

```js
bjs.css(key: string, value: string): this
```

## 获取全部样式

```js
bjs.css(): object
```

## 设置大量样式

```js
bjs.css(styles: object): this
```